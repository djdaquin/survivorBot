const Contestant = require('./contestant.js');

// Game should take an array of actions and return a game state.
// Should it take just actions or actions and characters?
// Probably game, actions, and characters/contestants

const Game = function () {
  return {};
}


module.exports = Game;
